1. Start by opening your command prompt. Navigate to the directory containing your code and text data with the
commands cd, ls, etc. For windows type "cd <file location>"

2. Once in the right directory, compile your code by typing "gcc FileReader.c -oreader"

3. To run the file type "./reader -d simple-text.txt" If that doesn't work type "reader -d simple-text.txt"