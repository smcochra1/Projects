To run this program, JavaFX needs to be installed and configured on your IDE. 

First, run the RPSServer file, then run the RPSClient file twice, and you will have two windows 
open that are connected to the server. 